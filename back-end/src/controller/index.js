const saleProductController = require('./SaleProduct');
const LoginController = require('./login.controller');
const UserController = require('./user.controller');

module.exports = {
    saleProductController,
    LoginController,
    UserController,
};
